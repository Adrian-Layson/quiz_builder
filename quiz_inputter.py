# Print title of the program

#Initialize a loop that:
    # Ask for question and 4 choices
    # Ask which choice is correct
    # Format and add the quiz to the list
    # Ask if user wants to add another
    #If not, stop

# Write all quiz entries to a text file
# Show "Quiz saved"
# End program